/*#pragma once

#include <string>

class Appointment {
public:
    Appointment(std::string patientId, std::string date, std::string time)
        : patientId(patientId), date(date), time(time) {}

    std::string getPatientId() const { return patientId; }
    std::string getDate() const { return date; }
    std::string getTime() const { return time; }

    void setDate(const std::string& newDate) { date = newDate; }
    void setTime(const std::string& newTime) { time = newTime; }

private:
    std::string patientId;
    std::string date;
    std::string time;
};*/
